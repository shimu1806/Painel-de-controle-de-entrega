faça as adaptaões necessárias em meu scipt para que ele funcione da seguinte forma. É necessário que ele busque os dados no endpoint http://127.0.0.1:8000/fetch_data/ de maneira que, a qualquer alteração realizada no endpoint, ele atualze a tabela. A tabela deve ser alimentada com as seguintes regras, os itens devem seu uma lista que são exibidos 25 por página; a tabela deve ter a funcionalidade de ordenar os itens de toda a tabela de menor para menor com o clique no cabeçalho, muito importante que ele recarregue a tablea, pois, nessa ordenação ele deve fazer a ordenação da lista como um todo, considerando o endpoint, não a página.